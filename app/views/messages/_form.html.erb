<%= simple_form_for [chat, message], html: { class: "chat-input d-flex align-items-center" } do |f| %>
  <!-- Champ texte -->
  <%= f.input :content,
        placeholder: "Pose ta question !",
        required: true,
        label: false,
        wrapper_html: { class: "flex-grow-1 mb-0" },
        input_html: { class: "form-control border-0 shadow-none" } %>
  <!-- Bouton envoyer -->
  <%= f.button :submit, '➤', class: "btn btn-primary ms-2" %>
<% end %>
