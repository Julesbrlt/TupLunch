<H2>My account</H2>
  <div class="col-md-4 mb-3">
    <div class="card shadow-sm">
      <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0"><%= @profile.name %></h5>
        <%= link_to "⚙️", edit_profile_path(@profile), class: "btn btn-outline-secondary btn-sm" %>
      </div>
      <div class="card-body">
        <p class="card-text mb-1"><strong>Préférences :</strong></p>
        <p class="card-text text-muted"><%= @profile.food_preferences %></p>
      </div>
    </div>
  </div>

<H2>My recipes(nombre de recettes)</H2>
<div class="container mt-4">
  <div class="row">
    <% @recap_recipes.each do |recipe| %>
      <div class="col-md-4 mb-3">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title"><%= recipe.recipe.name %></h5>
            <p class="card-text"><%= recipe.recipe.description %></p>
               <%= link_to "show this recipe", recipe_path(recipe.recipe_id)%>
              <%= link_to "🚮", recap_recipe_path(recipe), data: { turbo_method: :delete, confirm: "Avez-vous effectué cette recette ?" } %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

<H2>My favorites</H2>
<div class="container mt-4">
  <div class="row">
    <% @favorites.each do |favorite| %>
      <div class="col-md-4 mb-3">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title"><%= favorite.recipe.name %></h5>
            <p class="card-text"><%= favorite.recipe.description %></p>
               <%= link_to "show this recipe", recipe_path(favorite.recipe_id)%>
               <%= link_to "🚮", recipe_favorite_path(favorite.recipe_id, favorite.id), data: { turbo_method: :delete, confirm: "Voulez-vous vraiment retirer cette recette de vos favoris ?"}%>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
